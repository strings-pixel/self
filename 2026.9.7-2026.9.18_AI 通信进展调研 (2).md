#### 行业内AI+通信研究进展：
1、华为：

1. **面向网络数字孪生的数据采集：申请：2024-05-24；公开：2025-01-02 专利 WO2025001669A1 — 数据采集方法、装置和系统**

[https://patents.google.com/patent/WO2025001669A1/zh](https://patents.google.com/patent/WO2025001669A1/zh)

**（1）技术概述**

提出了一种专门面向网络数字孪生（Network Twin）的数据采集方法、装置和系统。该专利的核心创新在于：通过增强现有网络管理系统的数据采集机制，从物理网络中采集**业务流量特征数据**（而非常规的设备级 KPI），用于构建和持续更新网络数字孪生体，使其能够模拟物理网络中的业务流传输行为，进而实现业务体验质量（QoE）评估、性能预测和故障检测。

**（2）系统架构**

系统由三层组成：**网络孪生服务消费者**（BSS/第三方应用）发起评估请求 → **网络孪生管理实体**解析请求、触发数据采集、构建/更新孪生体、返回评估结果 → **管理系统**（NMS/EMS）向网元下发测量任务并采集业务流量特征数据。

**（3）采集策略**

支持两种采集模式：**静态指示**（固定周期上报）和**动态指示**（根据数据变化频率自适应触发，变化快多采、变化慢少采），实现了采集开销与数据新鲜度的动态平衡。

**（4）研究启示**

该专利确立了"面向孪生体的业务级数据采集"这一范式：网络数字孪生不仅需要设备级数据，更需要反映真实业务流传输特征的细粒度数据，比如：

→ 确定需要采集的数据：VR 业务流特征数据（速率、时延、抖动）

→ 确定采集域：RAN 侧 + 核心网侧

→ 确定采集方向：下行

→ 确定评估指标：时延

同时，专利中**动态指示的自适应采集策略**是根据网络状态变化动态调整测试/采集的频率和内容。

1. **Attention-based Wireless Digital Twin：申请：2023-07-10；公开：2025-01-16 专利 WO2025010581A1 — ****基于注意力机制的无线数字孪生模型**

[https://patents.google.com/patent/WO2025010581A1/en?oq=WO2025010581A1](https://patents.google.com/patent/WO2025010581A1/en?oq=WO2025010581A1)

**（1）技术概述**

该专利面向无线网络数字孪生中的长期网络性能预测问题，重点解决两个困难：

- 传统模型通常只能预测较短时间范围内的网络状态；
- 模型虽然能够预测KPI，却可能对网络配置动作不敏感，即改变配置后预测结果变化不明显。

提出了一种基于注意力机制（Attention）的无线数字孪生性能预测方法。该专利的核心创新在于：**将数字孪生从被动的"网络镜像"升级为主动的"State → Action 决策模型"**。具体而言，模型获取物理网络的历史性能数据（按双层时间粒度组织：粗粒度的采集周期 + 细粒度的数据点），基于注意力机制学习历史数据中的模式，预测未来网络性能，并支持对不同网络配置方案进行虚拟评估和择优。

**（2）模型架构**

模型包含两类时间尺度：

- **小时级模式**：采用常规Transformer Encoder提取较短时间尺度的KPI变化；
- **天级模式**：采用专利提出的Attention-in-Attention，即“AttenInAtten”结构，提取跨天变化规律。

AttenInAtten包含两层注意力：

1. 第一层完成多个相邻时间段之间的子预测任务；
2. 第二层综合不同子预测任务的表示，生成更长期的预测结果。

**注意力机制可以同时在两个尺度上捕捉模式**。粗粒度捕捉长期趋势（如一天中不同时段的流量模式），细粒度捕捉短期波动（如毫秒级的信道变化）。

**（3）关键机制：以网络动作为Query**

该专利最关键的设计是将"计划执行的网络动作"作为注意力机制中的 **Query（查询）**，而非传统做法中将"当前网络状态"作为 Query。具体而言：

- **Key**：历史状态 + 历史动作的联合编码
- **Value**：执行该动作后实际观测到的网络状态
- **Query**：当前状态 + 拟执行的未来动作（经线性映射后编码）

例如，可以把动作定义为：

- 发射功率调整；
- 天线参数调整；
- 资源分配变化；
- 负载均衡或网络配置变化。

然后比较：

s^t+1(a=A)与s^t+1(a=B)

从而支持不同配置方案的虚拟评估。因此，对于**同一个当前网络状态**，若输入不同的拟执行动作，注意力权重会重新分配，模型将输出不同的预测结果。这使得数字孪生具备了 **"What-If"虚拟评估能力**——无需实际执行操作，即可在孪生体中预先比较不同配置方案的预期效果，择优执行。

**（4）研究启示**

- **从"被动观测"到"主动决策"**：该专利最重要的启示是，数字孪生不应只是物理网络的镜像，而应成为一个能从 State 推理出 Action 的智能决策体。**终端侧 QoE 感知：**当孪生体预测到 QoE 即将恶化时，不仅报告问题，还能主动触发测试动作来验证和干预。
- **State → Action 闭环可迁移到终端侧**：该专利的决策闭环在基站/网络侧，但同样的范式可以迁移到终端侧——终端采集本地 State（RSRP、SINR、吞吐量），用轻量 Attention 模型推理出 Action（何时触发主动测试、测试什么指标、测试多密集等等），通过类似机制预判不同测试策略对 QoE 感知精度的提升效果，从而在测试开销与感知精度之间做出最优权衡。
1. **Predictive blockage detection：2024-05-06；公开：2025-04-03 专利 WO2025066147A1 — 基于数字孪生的预测性信号遮挡检测方法及系统**

[https://patents.google.com/patent/WO2025066147A1/en](https://patents.google.com/patent/WO2025066147A1/en)

直接研究：利用通信网络的数字孪生，**预测未来可能发生的通信遮挡**。

不是：blockage 发生了 → 检测。

而是：**根据当前环境和模型 → 预测马上要发生 blockage**。

**（1）技术概述**

提出了一种基于数字孪生的预测性信号遮挡检测（Predictive Blockage Detection, PBD）方法。核心思想是：将物理世界中的实体（基站、终端、建筑物、车辆等）映射为数字副本（D-Reps），通过预测各实体在未来时刻的位置，提前判断是否会出现信号遮挡，而非等遮挡发生后再检测。系统维护一个风险地图（Risk Map），按可配置的周期对脆弱区域执行 PBD，评估遮挡风险等级并生成解决机制（天线切换、频率切换、波束赋形更新等）。

**（2）核心机制：D-Reps + 未来位置预测 + 遮挡判断**

预测遮挡的核心流程分为三步：

- **第一步：识别脆弱区域（Vulnerability Region）**。脆弱区域是指网络中可能发生信号遮挡的地理区域，由系统中某个 D-Rep 发起 PBD 请求时指定，通常为该 D-Rep 对应实体当前所处位置或正在移动前往的区域。
- **第二步：检索实体数据**。PBD 请求触发后，系统连接脆弱区域内所有 D-Reps 所在的网络节点，检索这些 D-Reps 对应的实体数据（位置、运动轨迹、物理属性等）。
- **第三步：预测遮挡**。基于检索到的实体数据，**预测各 D-Reps 对应实体在未来时刻的位置，并判断这些未来位置是否存在遮挡物（如建筑物、移动车辆、甚至其他用户终端）对信号链路造成阻塞**。若预测存在遮挡，则发出遮挡告警。

PBD 并非全时全网执行，而是采用**按需触发**策略：仅在某个 D-Rep 主动发起请求时（通常对应实体正进入或即将进入信号复杂区域），才检索该区域内相关 D-Reps 的实体数据并执行预测。不同脆弱区域配置不同的检测周期——高速移动区域周期短（如 10 秒），静态区域周期长甚至不触发。该设计在预测精度与计算开销之间实现动态权衡，但**在高密度移动场景（如城市道路）中，对大量移动实体的实时轨迹追踪和频繁遮挡计算仍面临较大开销挑战**。

**（3）风险地图与周期性检测**

系统维护一个覆盖多个脆弱区域的**风险地图（Risk Map）**。每个脆弱区域具有独立的检测周期（Periodicity），系统按周期自动触发 PBD 请求。每次 PBD 执行后，根据预测结果更新该区域的**遮挡风险等级**。风险等级的判定不仅考虑遮挡预测结果，还综合该区域内的可用物理资源（信道频率、带宽、资源块、天线能力、感知数据可用性等），以评估应对遮挡的能力。

**（4）告警与解决机制**

当预测到遮挡时，系统生成的告警信息包含**告警时间窗口**（指示遮挡预计持续时长），并发送给脆弱区域内的相关 D-Reps 或对应的真实实体。同时，系统生成**解决机制**供目标网络实体执行，可选方案包括：天线切换（Antenna Handover）、信号频率切换、波束赋形更新、网络实体位置重调等。

**（5）研究启示**

该专利确立了"预测性遮挡检测"这一范式，即在问题发生前主动感知。可以把遮挡预测与主动测试结合起来：

- 低风险区域减少波束扫描；
- 不确定性较高时启用额外传感器或深度测试；
- 根据预计遮挡持续时间决定是否切换；
- 将误报造成的切换成本加入优化目标。

研究目标不应只是遮挡分类准确率，而应同时评价：

- 提前预警时间；
- 遮挡事件漏检率；
- 误触发切换次数；
- 波束测量开销；
- 用户吞吐率和中断时间。
1. **Model-aware network measurement：申请：2023-09-27；公开：2025-03-28 专利 CN119728456A — 网络管理方法及装置**

[https://patents.google.com/patent/CN119728456A/zh](https://patents.google.com/patent/CN119728456A/zh)

**（1）技术概述**

该专利针对 AI 预测用于网络管理时的一个根本问题：**AI 预测结果可能不准确，基于不准确的预测执行网络管理操作反而会恶化网络性能和用户体验**。为解决这一问题，专利提出了一种"模型感知"的网络管理方法：为每个网络管理场景维护多个候选预测模型，在获取当前测量数据后，各模型独立给出预测结果，同时输出每个预测结果的**质量指标（Quality Index）**，网络管理节点仅采纳质量达标的预测结果来执行操作。

**（2）两节点协作架构**

系统由两个逻辑节点组成：

- **第一节点（预测节点）**：获取与场景匹配的参考数据（Reference Data）和当前测量信息，通过至少一个预测模型分别处理当前测量信息，得到各模型的当前预测信息，并基于参考数据确定各预测信息的质量指标，将预测信息连同质量指标发送给第二节点。
- **第二节点（执行节点）**：接收各模型的预测信息及质量指标，筛选出质量指标符合质量条件的预测信息，据此执行网络管理操作（如网络节能、负载均衡、移动性优化等）。

整体流程：当前测量 → 多个候选模型 → 多个预测结果 → 质量评估 → 可信预测筛选 → 网络管理

**（3）质量指标的三种评估方式**

专利提供了三种评估预测质量的方法，可根据场景灵活选用：

| **评估方式** | **原理** | **适用场景** |
| --- | --- | --- |
| **预设测试集** | 用已知输入-输出对的测试集跑一遍模型，比较预测值与真实值，用测试集上的误差近似当前预测的质量 | 测试集覆盖当前场景时 |
| **历史预测质量** | 直接用该模型在历史预测中的质量指标（如过去一段时间的 MAE）作为当前预测的质量估计 | 场景变化缓慢、模型表现稳定时 |
| **预测辅助信息** | 根据模型的属性信息（如模型类型、训练数据分布）和运行环境（如当前负载、信道条件）推断当前预测的可靠性 | 测试集不可用且历史数据不足时 |

**（4）研究启示**

该专利的核心贡献在于将"模型预测"与"预测质量评估"解耦，引入了一个**评估层**：在信任预测结果之前，先评估这个预测有多可靠。启示：① 在 QoE 预测中，模型应同时输出预测值及其置信度/不确定性；② 当预测置信度低于阈值时，可触发主动测试来获取真实测量数据以校准模型，形成"预测→评估→不确信→主动测量→更新模型"的闭环；③ 多候选模型 + 质量筛选的架构，可解决终端侧单一模型在不同场景下泛化能力不足的问题。

##### 2、高通（Qualcomm）

1. **AI 模型训练的数据动态采集：申请：2025-09-29；公开：2026-04-09 专利 WO2026075914A1 — 面向网络模型训练的事件驱动数据采集**

[https://patents.google.com/patent/WO2026075914A1/en](https://patents.google.com/patent/WO2026075914A1/en)

**（1）技术概述**

该专利提出了一种**事件驱动的训练数据采集机制**，用于高效收集训练网络侧 AI 模型所需的数据。网络侧部署了一个 AI 模型，用于预测移动性事件（如用户即将发生切换、信号即将恶化等），该模型的训练需要大量来自 UE 的实测数据。传统做法中，UE 周期性上报测量数据，无论这些数据对模型训练是否有价值，导致大量冗余上报。该专利的核心创新在于：**UE 持续进行物理层测量但仅在本地缓存，只有当特定移动性事件触发时，才将事件前后的缓存数据打包上报给网络**，从而将上报开销集中在高质量训练样本上。

**（2）核心机制：持续测量 + 事件触发上报**

UE 内部持续运行（始终执行，开销极小）：

→ 测量 RSRP、SINR 等物理层指标

→ 维护一个本地滚动缓冲区（保留最近若干秒的测量数据）

→ 检测事件是否发生（RSRP 骤降？波束失败？切换？）

事件发生前：测量数据存在本地缓冲区，不上报

事件发生时：触发 → 从缓冲区取出"事件前"数据 + 继续采集"事件后"数据 → 打包上报

上报内容：事件前一段窗口 + 事件后一段窗口的完整测量数据

前窗口和后窗口的时长/采样数量由网络侧下发的上报参数（Reporting Parameters）配置。

**（3）触发事件类型**

专利列出了多种可触发数据采集的事件，均与移动性相关：

| **事件类型** | **具体示例** |
| --- | --- |
| 信号质量变化 | RSRP 变化超过阈值 |
| 链路失败 | 无线链路失败（RLF）、波束失败（Beam Failure） |
| 切换事件 | 切换（Handover）发生 |
| 传输异常 | RLC 重传次数超过阈值 |
| 信道条件变化 | 多普勒指标超过阈值、时延扩展超过阈值 |
| 接入失败 | LBT 失败次数超过阈值、信道接入尝试次数超过阈值 |

**（4）关键设计：训练数据与常规测量数据的区分**

一个 UE 可能同时承担两种测量任务——常规网络测量和 AI 训练数据采集。专利通过以下机制区分两者：

- **不同标识符（ID）**：训练数据采集配置和常规测量配置使用不同的 ID，UE 上报时携带对应 ID，网络侧据此判断该上报数据是用于 AI 训练还是常规监测
- **显式标记**：控制消息中明确指示该上报参数是否用于 AI 训练，UE 据此决定是否需要额外记录（如本地缓存日志）
- **采集窗口配置**：可配置事件前采集时长/数量 + 事件后采集时长/数量，确保捕获事件发生前后的完整上下文

**（5）研究启示**

该专利确认了一个重要趋势：**AI 模型的训练数据采集正在从"全量采集"向"事件驱动、按需采集"转变**。启示：① 主动测试的触发条件可以借鉴移动性事件（如切换、信号骤降）作为触发点，而非仅依赖固定周期；② 测试数据应包含事件前后的时间窗口，以捕获完整的动态变化过程；③ 训练数据与常规数据的分离机制，可迁移到终端侧 QoE 预测模型的数据采集中——区分"用于训练模型的测试数据"和"用于实时决策的监测数据"。

1. **AI/ML measurement 的“质量标签”：申请：2025-09-24；公开：2026-05-21 专利 WO2026106710A1 — AI/ML信道测量的质量信息**

[https://patents.google.com/patent/WO2026106710A1/en](https://patents.google.com/patent/WO2026106710A1/en)

**（1）技术概述**

该专利解决了一个基础性问题：当信道测量数据被送入 AI/ML 模型用于定位或感知时，测量值本身并不包含"这个测量有多可靠"的信息。例如，一个 RSRP 测量值 -90dBm，是在高 SNR 理想条件下测得的，还是在强干扰、低 SNR 条件下测得的，AI 模型无从得知——但它直接决定了这个测量值该被多大程度地信任。该专利提出：**无线设备在上报信道测量数据时，应同时上报该测量数据的质量信息（Quality Information）**，使网络侧 AI/ML 模型能够根据测量质量调整对输入数据的置信度，从而做出更准确的推断。

**（2）核心思想：从"测量值"到"测量值 + 质量标签"**

- 传统模式：

UE 上报：RSRP = -90dBm

AI 模型：不知道这个值靠不靠谱，一视同仁地处理

- 本专利模式：

UE 上报：RSRP = -90dBm，质量信息：SNR = 3dB（低置信度）

AI 模型：SNR 只有 3dB，这个测量可能不太准，降低其权重

**（3）质量信息的表示形式**

专利提供了多种质量信息的表达方式，可根据场景选择：

| **质量信息类型** | **含义** |
| --- | --- |
| 数值范围 | 一个统一的质量分值（如 0-100） |
| SNR / SINR | 测量时的信噪比/信干噪比，直接反映测量可靠性 |
| 时延扩展（Delay Spread） | 多径效应的严重程度，影响信道估计精度 |
| 莱斯因子（Ricean Factor） | 视距分量与散射分量的比值，反映信道确定性 |
| 统计信息 | 多次测量的方差、均值等统计量 |

**（4）质量标签的粒度**

专利的一个关键设计是质量标签**可配置到不同粒度**，从上到下越来越细：

粗粒度 ─────────────────────────────────────────────────→ 细粒度

整个报告              频段级           TRP级     天线参考点级   参考信号资源集级   参考信号级   径级       采样级

（一个质量标签  （每个频段  （每个发射  （每个天线      （每个资源集       （每个参考   （每条      （每个

覆盖全部）    独立标签）  接收点独立） 参考点独立）      独立标签）          信号独立）  多径独立） 采样点独立）

网络侧可配置所需的粒度级别，也可指定优先级顺序。UE 根据自身能力选择能支持的粒度。粒度越细，质量信息越精准，但上报开销越大。

**（5）网络侧如何使用质量信息**

专利明确列出了网络侧收到质量信息后的用途：**训练 AI/ML 模型**（对低质量测量数据降权）、**AI/ML 预测推理**（根据质量信息调整预测置信度）、**监控 AI/ML 模型**（当输入质量持续偏低时触发模型更新）、**输入 AI/ML 模型**（质量信息作为模型输入特征之一）、**控制 AI/ML 模型**（当质量低于阈值时切换回非 AI 方法作为回退）。

**（6）研究启示**

该专利与前面华为 CN119728456A（预测质量评估）研究思路类似：华为是**模型输出端**加质量标签（“这个预测值有多可信”），高通是**模型输入端**加质量标签（“这个测量值有多可信”）。两者的共同趋势是：**AI/ML 驱动的网络管理正在从"裸数据"时代进入"数据 + 置信度"时代**。启示：终端侧 QoE 预测系统应同时考虑两端的质量标签——输入端（RSRP/SINR 等物理层测量附带的质量信息决定模型接收到的数据可信度）和输出端（模型预测的 QoE 值附带置信度决定是否触发主动测试）。当两端质量都低时，说明当前场景的数据基础和模型能力都不足以支撑可靠决策，这恰是触发主动测试获取高质量真实数据的信号。

1. **虚拟测量：申请：2023-01-18；公开：2024-07-18；授权：2026-05-12 专利 US12625275B2 — ****虚拟定位信号测量**

[https://patents.google.com/patent/US12625275B2/en](https://patents.google.com/patent/US12625275B2/en)

**（1）技术概述**

该专利提出了一种**虚拟测量值生成方法**，用于解决定位场景中测量数据缺失的问题。UE 在进行定位时，需要测量来自多个基站/TRP（发射接收点）的定位参考信号（PRS），但实际中常因信号遮挡、同频干扰、PRS 静默等因素导致某些预期的测量值缺失。该专利的核心思想是：**利用已有的同类型实际测量值，推断出缺失的虚拟测量值**，从而补全测量数据集，使后续定位算法能够正常工作。

**（2）核心机制：Sparse → Virtual**

- 传统做法（测量缺失时）：

预期测量 5 个 TRP 的 PRS 信号

→ 实际只测到 3 个（TRP1、TRP3、TRP5）

→ TRP2 和 TRP4 的测量缺失

→ 只能拿 3 个测量值去定位，精度下降或无法定位

- 本专利做法：

预期测量 5 个 TRP 的 PRS 信号

→ 实际只测到 3 个（TRP1、TRP3、TRP5）

→ 发现 TRP2 缺失 → 根据 TRP1 和 TRP3 的测量值推断 TRP2 的虚拟值

→ 发现 TRP4 缺失 → 根据 TRP3 和 TRP5 的测量值推断 TRP4 的虚拟值

→ 5 个测量值补全，定位算法正常执行

**（3）推断逻辑：同类型测量为基准**

专利强调虚拟测量值必须是**同一测量类型**：例如缺失的是 ToA（到达时间），则用已有的 ToA 测量值来推断，不能用 RSRP 来推断 ToA。推断方法可以是基于已有测量值之间的空间/时间关系进行插值、外推或模型预测。

**（4）研究启示**

该专利反映了高通在测量领域的一个重要演进方向：从 **Adaptive Measurement**（自适应选择测什么）进一步发展到 **Measurement Reconstruction**（测量缺失时推断补全）。这一思路对本研究有直接启示：在终端侧 QoE 感知中，主动测试可能因终端功耗、网络条件等限制无法覆盖所有需要的测量维度，此时可借鉴"虚拟测量"的思路——根据已获取的物理层指标（RSRP、SINR）推断未直接测量的传输层指标（如吞吐量、时延），从而在降低测试开销的同时保持 QoE 评估的完整性。

##### 3、苹果

1. **申请：2024-01-26；公开：2024-11-07；授权：2026-05-05 专利 ****US12621840B2 — 播放卡顿预期上报**

[https://patents.google.com/patent/US12621840B2/en](https://patents.google.com/patent/US12621840B2/en)

**（1）技术概述**

该专利提出了一种**从应用层到网络层的跨层卡顿预警机制**。传统做法中，视频播放卡顿（Playback Stall）发生后才被感知，网络侧无法提前干预。该专利的核心思想是：手机上的视频应用（如 Apple Music、Safari 视频播放）自身维护着播放缓冲区，当应用判断缓冲区即将耗尽、预测到即将发生卡顿时，主动生成一个"卡顿预期事件"（Playback Stall Expectation Event），通过基带处理器上报给蜂窝基站，使网络侧有机会提前调整无线资源配置来避免或减轻卡顿。

**（2）核心流程：App → 基带 → 基站**

当视频应用在播放过程中，其自身维护着一个播放缓冲区。应用持续监控缓冲区状态，当判断缓冲区即将耗尽时，生成一个"卡顿预期事件"，包含预计卡顿发生时间（如 2 秒后）和当前已缓冲的播放时长（如 2 秒）。该事件从应用层传递到手机的基带处理器（Modem 层），基带处理器将其封装为 RAN Visible QoE 指标，通过应用层测量报告消息上报给当前服务的蜂窝基站。基站收到该信息后，可根据整体网络运营情况自行决定是否调整该设备的无线资源配置（如增加调度优先级、分配更多资源块、调整 QoS 参数），以尝试避免或减轻卡顿。

**（3）关键设计要点**

- **上报方式**：使用 RAN Visible QoE（RAN 可见的体验质量指标）格式，通过应用层测量报告消息（Application Layer Measurement Report）承载
- **非强制性**：基站收到卡顿预期后**不一定**必须响应——专利明确说明这只是向网络"提供信息"，网络可根据整体运营情况自行决定是否及如何调整
- **包含的信息**：预计卡顿发生时间（How long until the predicted stall）、当前已缓冲的播放时长（Amount of playback time currently buffered）

**（4）研究启示**

这是苹果在用户体验感知领域的重要布局，其核心价值在于**打通了"应用层知道 QoE 即将恶化"和"网络层有能力干预"之间的信息壁垒**。传统网络中，应用层知道要卡了但没法告诉基站，基站能调资源但不知道谁需要帮助。该专利打通了这条通路。启示：① 应用层 QoE 感知（如视频缓冲状态）可以作为终端侧主动测试触发的重要参考信号——当应用层预测到 QoE 恶化时，可以协同触发网络层面的主动测量，获取更丰富的诊断数据；② 苹果的"卡顿预期"思路可以泛化为更通用的"QoE 恶化预期"上报机制，不仅限于视频播放，也可扩展到实时通信、云游戏等场景；③ 苹果的跨层设计（App → Modem → 基站）为终端侧主动测试系统的架构设计提供了参考——将应用层感知、基带层测量、网络层响应串联成完整的闭环。

1. **UE主动要求重新配置QoE测量 ****：优先权 2022-08-09；申请 2023-07-20；公开 2026-01-29 ****专利**** US20260032488A1 — UE 主动触发的应用层测量上报重配置**

**（1）技术概述**

该专利提出了一种由 UE 主动触发 QoE 测量上报重配置的机制。传统做法中，QoE 测量上报的配置（测什么、多频繁上报、何时暂停）完全由网络侧决定，UE 被动执行。该专利的核心创新在于：**UE 可根据自身状态（电量、移动性、隐私设置）和历史 QoE 测量结果，主动评估是否需要调整上报策略，并向网络侧发起重配置请求或直接通知网络侧其决定**，从而在保障 QoE 监测有效性的同时，优化 UE 的功耗和信道资源占用。

**（2）核心流程**

UE 对自身状态和/或历史 QoE 测量结果进行评估 → 发现需要调整上报策略（如电量过低希望减少上报频率、高速移动中希望暂停上报、隐私设置拒绝上报等） → 触发 QoE 测量重配置。专利提供了两种触发模式：

- **请求模式（Request）**：UE 向基站发送 QoE 重配置请求，表明调整偏好（暂停/恢复/挂起/修改），等待基站回复批准或拒绝后再执行
- **通知模式（Notification）**：UE 自行决定并执行调整，同时向基站发送通知告知其决定，基站可选择性回复确认

此外，基站也可在配置 QoE 测量之前主动向 UE 发送配置询问，UE 评估后回复是否接受；或在运行一段时间后基站主动询问 UE 是否需要恢复或调整 QoE 上报。

**（3）UE 触发重配置的典型场景**

| **触发条件** | **重配置动作** |
| --- | --- |
| UE 电量过低 | 降低上报频率或暂停上报 |
| UE 处于高速移动 | 暂停上报（移动中上报数据参考价值有限） |
| 隐私设置严格 | 拒绝 QoE 上报 |
| 历史 QoE 测量结果持续良好 | 降低上报频率（减少不必要的上报） |

**（4）两种 QoE 测量类型**

| **类型** | **内容可见性** | **用途** |
| --- | --- | --- |
| 容器式 QoE（Container-based） | 基站不可见，封装后转发给管理实体 | OAM 分析 |
| RAN 可见 QoE（RAN visible） | 基站可见 | 实时无线资源管理优化 |

**（5）研究启示**

该专利确立了 UE 在 QoE 测量中的**主动角色**——UE 不再是网络侧指令的被动执行者，而是根据自身状态主动参与测量策略的决策。启示：① 主动测试的触发条件不应仅考虑网络状态，还应纳入终端自身状态（电量、温度、移动速度、用户隐私偏好）作为决策因子；② 苹果的"请求/通知"双模式为主动测试触发机制提供了协议设计参考——测试策略调整可先向网络侧请求（请求模式），也可在紧急情况下先执行后通知（通知模式）；③ 该专利与前述苹果 US12621840B2（卡顿预期上报）共同构成了苹果在终端侧 QoE 感知体系中的两个关键环节——前者是"终端主动报告 QoE 问题"，后者是"终端主动控制测量节奏"，两者结合实现了终端侧 QoE 感知的完整闭环。

#### 研究方向：

##### 1、AI改进“测试方法”

1. **研究思路：**AI驱动的动态网络测试与自适应测量方法

传统网络测试通常预先规定采样周期、指标集合、测试路线和触发阈值。AI改进测试方法研究的是如何根据网络状态、业务风险和模型不确定性动态调整测试过程。算法的直接输出应当是测试动作，例如下一次采样时间、需要补测的KPI、应访问的测试位置或是否启动高成本深度测试，而不是单纯输出RSRP或QoE预测值。

**调研路线：**第一条是事件驱动采集，围绕切换、遮挡、卡顿等关键事件形成完整的事件前后数据窗口。第二条是价值驱动采集，根据预测风险和不确定性决定测量频率。第三条是测试执行与压缩，包括KPI压缩等。

第一条是**预测性事件驱动采集**。利用AI预测切换、遮挡、卡顿、波束失败或无线链路失败等事件，在事件发生前提前提高采样频率、增加测量指标，并保留事件发生后的恢复过程，从而形成完整的事件前后数据窗口。重点研究如何确定触发时机、窗口长度和指标集合。

第二条是**风险与不确定性驱动的主动测试**。根据QoE下降风险、模型预测误差和不确定性判断哪些时间、位置或指标最值得补测。网络状态稳定且模型置信度较高时减少测量；预测风险或不确定性升高时增加测量，从而在有限测试预算下提高异常捕获能力和数据有效性。

第三条是**预算约束下的测试资源编排**。综合考虑测试时间、终端功耗、空口上报开销、测试路线和数据存储成本，联合优化采样频率、KPI集合、测试位置、提前停止条件及重要性感知压缩。稳定区间可以降低采样或压缩精度，异常区间则保留更完整的原始数据和协议层日志。

1. **调研内容**

**（1）****华为 WO2025001669A1 面向网络孪生的数据采集**

**（2）****高通 WO2026075914A1 面向模型训练的事件化数据采集**

**（3）****苹果**** US20260032488A1 ****终端触发****QoE****测量重配置**

**（4）AI-Driven Mobility Management for High-Speed Railway Communications: Compressed Measurements and Proactive Handover 面向高速铁路通信的AI驱动移动性管理：压缩测量和主动切换**

[https://arxiv.org/html/2407.04336v3](https://arxiv.org/html/2407.04336v3)

- **作者**：Wen Li, Wei Chen, Shiyue Wang, Yuanyuan Zhang, Michail Matthaiou 等
- **出处**：IEEE Transactions on Vehicular Technology / IEEE 期刊，2025年
- **机构**：北京交通大学（轨道交通控制与安全国家重点实验室）

**a. 技术概述**

该论文针对高铁通信中移动性管理的核心挑战——**列车高速移动导致频繁切换和信道剧烈波动**——提出了一套 AI 驱动的解决方案。论文包含两个核心模块：① 利用压缩感知（Compressed Sensing）对测量数据进行**压缩采集**，用少量测量样本重建完整的信道状态，从而降低高铁场景下的测量和反馈开销；② 基于 AI（LSTM 等）对信道和吞吐量进行**预测**，提前判断最优切换时机和目的小区，实现**主动切换（Proactive Handover）**，而非传统的事后反应式切换。

**b. 核心思路**

高铁场景的特殊性——列车路线固定、速度已知——为 AI 预测提供了天然优势。论文的核心逻辑是：

传统做法：

列车高速移动 → 信道快速变化 → 频繁测量 → 大量测量开销

→ 信号变差后才触发切换 → 切换延迟 → 掉话/卡顿

本文方案：

压缩采集少量测量 → 重建完整信道信息 → LSTM 预测未来信道状态

→ 提前判断"什么时候、切到哪个小区"

→ 主动发起切换 → 零中断/低中断

**c. 两个关键技术模块**

**模块一：压缩测量（Compressed Measurements）**

高铁信道在空间-时间域具有稀疏性（大部分能量集中在少数路径上）

- 利用压缩感知理论，只需采集远少于奈奎斯特采样率要求的测量样本
- 通过重建算法恢复完整的信道状态信息（CSI）
- 效果：大幅降低测量开销和上行反馈带宽需求

**模块二：主动切换（Proactive Handover）**

- 利用 LSTM 网络学习高铁历史信道变化的时序模式
- 输入：压缩重建的信道状态 + 列车位置/速度
- 输出：预测的未来吞吐量/信号质量 + 最优切换时机和目的小区
- 效果：在信号恶化前完成切换准备，实现接近零中断的移动性管理

**d. 研究启示**

论文的压缩测量思路与高通 WO2026075914A1（事件驱动采集）形成了两个互补的减少测量开销的思路——前者是"测少但重建全"，后者是"只测关键时刻"；论文同时处理"信道预测"和"切换优化"两个问题，为后续研究将"QoE 预测"和"主动测试触发"耦合设计提供了方法参考——两者不是独立的，预测结果的不确定性正是触发测试的信号。

**（5）Lossy Compression of Cellular Network KPIs：2026 年 1 月 arXiv:2601.23105v1 — 蜂窝网络 KPI 的有损压缩（米兰理工大学 & 贝加莫大学）**

[https://arxiv.org/html/2601.23105v1](https://arxiv.org/html/2601.23105v1)

**a. 技术概述**

该论文系统研究了对蜂窝网络 KPI 进行有损压缩的可行性与影响。研究基于欧洲某中型城市约 3000 个 LTE 小区的实测数据（2023 年 10 月，小时级采样），覆盖三类核心 KPI：下行流量、PRB 占用率和活跃用户数。论文的核心发现是：使用标准的有损压缩方案（预测 + 量化 + 熵编码），用 3-4 bit/样本即可达到约 30 dB 的 SNR，相比 32 位浮点表示实现 8-10 倍压缩，且**对下游分析任务（流量聚合、流量预测）几乎无影响**。

**b. 四种压缩方案对比**

论文比较了四种经典的预测/变换域压缩方案：

| **方案** | **原理** | **性能** |
| --- | --- | --- |
| **PCM（脉冲编码调制）** | 直接对样本量化，无预测无变换 | 最差（基准线） |
| **DPCM（差分编码）** | 对相邻样本差值做量化，利用短期时序相关性 | 中等，优于 PCM |
| **DCT（离散余弦变换）** | 按周期（168 小时 = 一周）分块做变换，系数独立量化 | 优，接近 KLT，且无需训练 |
| **KLT（Karhunen-Loève 变换）** | 基于数据协方差矩阵的最优正交变换 | 最优，但需要从训练数据估计变换矩阵 |

关键结论：DCT 接近 KLT 的性能，但无需训练阶段，在实际部署中最具可行性。

**c. 核心发现：聚合消除量化噪声**

论文的重要发现是：**空间聚合天然地衰减量化噪声**。当对多个小区的 KPI 进行求和聚合时，各小区的量化误差随机叠加、相互抵消，而有用的流量分量相干叠加。例如，聚合 1000 个小区时，即使单小区 SNR 仅 15 dB（对应 1-2 bit/样本），聚合后的 SNR 仍可达 30 dB。这意味着：对单小区可大胆压缩，在核心网级别的聚合视图中几乎无损还原。

**d. 下游任务影响**

基于 MWS（Median Weekly Signature，中位数周特征）预测器进行流量预测实验的结果表明：当单小区 SNR 达到约 30 dB 时，基于压缩数据的预测 RMSE 与基于原始数据的预测 RMSE 基本一致，压缩对预测精度无实质影响。

**e. 研究启示**

这篇论文为本研究方向从"测量开销"维度提供了支撑：① 终端侧采集的物理层/应用层指标必然是海量时序数据，有损压缩可以在不损害下游 QoE 评估精度的前提下大幅降低存储和传输开销；② "聚合消除噪声"的发现意味着，当主动测试数据以小区/区域级别聚合使用时，单个终端的测量数据可以更激进地压缩，这为轻量级终端侧数据采集提供了理论依据；③ 论文采用的"压缩 vs 下游任务性能"联合评估范式可迁移到本研究——不仅关注测试数据本身的压缩率，更要关注压缩后的数据对 QoE 预测精度的实际影响。

**（6）Automatic Generation of Digital Twins for Network Testing 自动生成用于网络测试的数字孪生**

[https://arxiv.org/abs/2510.03205?utm_source=chatgpt.com](https://arxiv.org/abs/2510.03205?utm_source=chatgpt.com)

- **作者**：Shenjia Ding、David Flynn、Paul Harvey（格拉斯哥大学计算科学学院 & 工程学院）
- **发表**：IEEE ICDCS 2025 Workshops（ANMS Workshop）
- **时间**：2025年10月

**Automated Digital Twin Generation for Network Testing: A Multi-Topology Validation 用于网络测试的自动化数字孪生生成：多拓扑验证**

[https://ieeexplore.ieee.org/document/11587859](https://ieeexplore.ieee.org/document/11587859)

- **作者**：Shenjia Ding、David Flynn、Paul Harvey（格拉斯哥大学）
- **发表**：IEEE ICC 2026，DOI:10.1109/ICC59461.2026.11587859
- **时间**：2026年5月

**a. 技术概述**

电信网络正向"自主网络"演进，软件承担越来越多的网络管理和运维功能，但全球仅 39% 的软件项目成功交付，网络控制软件在上线前必须经过严格的测试验证。现有测试工具各有短板：物理硬件精度高但慢且僵化；仿真器（ns-3、Mininet 等）灵活但执行慢，面对海量参数组合时时间成本过高；数字孪生（DT）兼具高精度和高速度，但 DT 构建依赖人工专家——每个测试场景都要手动设计模型架构、反复调参训练，无法规模化。论文提出用 **AutoML**（AutoGluon 和 Auto-sklearn）自动完成从数据预处理、特征工程、模型选择到超参调优的全流程，从少量训练数据中自动生成 DT，最少化人工干预。论文将其生成的 DT 称为"单元孪生（Unit Twin）"——只捕捉特定网络功能或组件的行为（如一条路径的时延与配置参数的关系），而非一个包罗万象的全网 DT，多个单元孪生可组合服务于系统级测试。

**b. 实验设计**

论文 1 使用 Mininet 搭建菱形网络拓扑（含两条路径，链路带宽 25-125Mbps 可调，路由器队列大小 50-150 可调），客户端向服务器请求 100MB 文件，记录两条路径的传输时延。从 194,481 个全量配置组合中仅用间隔采样策略抽取 400 个作为训练样本，AutoML 输入带宽和队列大小，输出路径时延预测值。训练时间上限 300 秒，使用 AutoGluon 的 “good_quality” 预设。

还设计了噪声鲁棒性实验：用 ns-3 重新生成数据，加高斯噪声（标准差 1，均值 0），再用 Savitzky-Golay 滤波降噪，模拟真实网络噪声环境。

论文 2 将验证扩展到多种网络拓扑，并将生成的 DT 与网络服务控制器集成以优化配置参数。

**c. 核心结论**

| **结论** | **数据** |
| --- | --- |
| DT 推理速度远超仿真 | DT 单次执行 0.0167s，Mininet 仿真 8.5316s，快约 500 倍（论文 2 中更快约 25,000 倍） |
| 全流程效率提升巨大 | 400 训练样本 → 预测 194,481 测试用例，全流程 3.4 小时；纯 Mininet 仿真需 900+ 小时 |
| 预测精度高 | AutoGluon：Path 1 加权集成模型 99.79%，Path 2 XGBoost 91.57%；Auto-sklearn：99% 和 92% |
| 多拓扑泛化 | 论文 2 在多种拓扑下均保持高精度 |
| 噪声影响可控 | 高斯噪声导致准确率下降约 12%，滤波后恢复至接近原始水平 |

**d. 研究启示**

该工作是已知首个将 AutoML 应用于网络 DT 自动生成的研究。启示：① 终端侧的 QoE 预测模型可考虑用 AutoML 自动生成，不同楼宇、不同时段的网络行为模式不同，AutoML 可针对每个场景自动产出最优模型结构，而非固定手工设计 LSTM/Transformer；② "400 样本覆盖 19 万测试"的高性价比数据，为主动测试中"以少量精心选择换取全量覆盖"提供了实证支撑；③ 多拓扑泛化验证说明自动生成的 DT 在新场景下可通过少量数据快速重训练适配。

##### **2、AI如何分析“测试结果”**

1. **调研思路**

网络测试会产生连续的无线信号、网络性能和业务体验数据。传统分析主要依靠固定阈值、统计报表或人工对比曲线，难以处理动态场景中的快速变化、指标关联和模型失效问题。

本方向重点研究AI如何从一段已经采集的动态网络测试数据中，预测短期网络状态、识别异常变化，并判断分析结果是否可信。预测不是为了覆盖所有网络规划和流量调度问题，而是作为理解测试结果的工具：模型先学习正常情况下信号或性能应当如何变化，再利用实际结果与预测结果之间的偏差发现异常。

- **短时状态预测**：根据最近一段测试数据，预测未来几秒或几十秒的信号、吞吐量或QoE。
- **异常变化识别**：根据预测残差或时序特征，识别信号突降、吞吐下降、时延突增、切换异常或业务卡顿。
- **结果可信度评估**：给出预测区间或异常置信度；当数据缺失严重或出现未知场景时，降低置信度或拒绝判断。
1. **调研内容**

**（1）****华为 CN119728456A 多模型预测质量驱动的网络管理**

**（2）****高通 WO2026106710A1 AI定位与感知测量的质量信息**

**（3）****高通 US12625275B2 定位测量缺失时的虚拟测量**

**（4）爱立信：持续监测通信网络中AI模型是否已经失效**

**优先权：2021-08-05；申请：2022-08-05；公开：2024-07-18；授权：2025-11-04；状态：已授权、有效**

**专利：**[**US12463883B2—Method for monitoring performance of an AI/ML model or algorithm**](https://patents.google.com/patent/US20240243984A1/en?utm_source=chatgpt.com)

**a. 技术概述**

该专利提出了一种在无线通信网络中持续监测 AI/ML 模型性能的方法，核心解决一个现实问题：**AI 模型部署上线后，运行环境可能随时间发生变化（如网络负载模式迁移、用户行为改变、设备升级等），模型性能会逐渐退化甚至失效，但网络侧此前缺乏标准化的监测机制来发现这一问题**。专利基于 3GPP TR 37.817 定义的 RAN 智能功能框架（数据采集 → 模型训练 → 模型推理 → 执行者），在现有框架中新增了一个显式的"模型性能监测"环节，使得 AI/ML 模型的退化可以被检测、度量并触发重训练。

**b. 核心流程**

监测节点向数据采集节点订阅历史数据（可按区域、时间窗口、UE 类型、切片等多种条件过滤） → 数据采集节点按订阅条件返回历史数据 → 监测节点比较"当时模型的预测值"和"当时的真实测量值"，计算性能指标（准确度、偏差、不确定性水平） → 判断是否满足重训练准则（如准确率连续一段时间低于阈值、平均误差超过阈值等） → 若满足，触发模型重训练，用最新数据训练新版模型替换旧版。

**c. 重训练触发条件的多样性**

专利支持多种判定逻辑：单阈值触发、双阈值区间触发、性能指标持续低于阈值一段时间后触发、性能指标在预定义时段内的平均值或方差低于阈值后触发。

**d. 研究启示**

该专利从爱立信的角度确认了一个重要趋势：**AI/ML 模型的生命周期管理正在从"训练一次、部署即忘"走向"持续监测、按需重训练"**。终端侧的 QoE 预测模型也会随时间退化（用户行为变化、基站配置调整等），需要持续的闭环监测。具体启示：① 专利中"单阈值 + 双阈值 + 时间持续 + 统计矩"的多层级重训练判断准则，启示主动测试触发条件设计——不仅"预测置信度低"触发测试，"连续 N 次预测质量下降"也可作为触发条件；② 专利对历史数据的按需订阅和过滤机制（按区域、按服务类型、按时间窗口），启示终端侧主动测试的采样策略——只取与当前场景相关的高质量历史数据做模型评估，而非全量对比。

**（5）AT&T与加州大学圣迭戈分校：从用户投诉反向定位网络侧或终端侧根因**

**优先权及申请：2022-09-30；申请公开：2024-04-04；授权：2025-11-11；状态：已授权、有效**

**专利：**[**US12470955B2—Automated AI/ML event management system**](https://patents.google.com/patent/US12470955B2/en?utm_source=chatgpt.com)

**a. 技术概述**

用户投诉网络卡顿或无法通话时，运营商客服需要在几分钟的通话时间内判断"是基站出问题了还是手机出问题了"，判断不准则工单在二线团队之间反复流转，解决周期长。该专利提出了一套**双层 ML 模型驱动的自动化根因定位系统**：第一层是**小区级模型（Cell-level Model）**，分析基站级别的网络指标（如 RRC 连接数异常波动），检测是否存在影响整片区域的网络异常事件；第二层是**UE 级模型（UE-level Model）**，将小区级模型的输出与单个用户的性能指标结合，判断该用户的问题是"网络侧故障导致的"还是"手机/账户问题导致的"。两个模型协同工作，在客服接到电话的几分钟内自动给出根因判断和派单建议。

**b. 工作流程**

用户投诉 → 提取投诉用户所在小区 → 小区级模型检测该小区是否存在网络异常事件（如 RAN 故障、核心网问题） → 如有，将异常事件信息传给 UE 级模型 → UE 级模型结合用户的历史性能数据，判断用户症状是否与网络异常相关 → 若相关，判定为网络侧问题，自动派单给网络支持团队；若不相关，判定为终端侧或账户问题，由客服引导用户检查设备或账户设置。对于已知类型的网络异常事件，系统还能自动识别受影响的其他客户并预估影响范围，提前通知或派资源。

**c. 解决的三个关键问题**

专利明确提出该系统要同时回答三个问题：① 用户所在小区是否存在网络异常？② 用户的症状与附近网络异常是否相关？③ 如果不相关，问题是终端侧还是账户侧？只有同时回答这三个问题，才能准确判断"是网络的问题还是用户自己的问题"。

**d. 关键技术挑战与解决思路**

专利指出了设计此类系统的三个挑战并给出对应方案：① 小区级和 UE 级事件之间存在复杂的时空关联（cell-to-cell 和 cell-to-UE），用 ML 模型建模这种关联；② 缺乏足够的标注数据（真实根因标签获取昂贵），在有限标签下训练；③ 每个小区和每个用户都有独特特征，模型需要能处理这种个性化差异。

**e. 研究启示**

该专利从运营商视角证明了"从用户投诉反向定位根因"的可行性和商业价值——加快投诉处理速度、减少工单流转次数。启示：在终端侧 QoE 感知和主动测试中，同样面临"信号差了到底是网络问题还是终端问题"的判断需求——如果终端能自动判断是网络侧异常（可上报）还是终端自身问题（无需上报或需本地处理），就能进一步减少不必要的上报开销。

**（6）**[**QoE Prediction in RIC-Assisted Wireless Real-Time Video Transmission System With Transformer**](https://ieeexplore.ieee.org/document/10973203)** 基于 Transformer 的 RIC 辅助无线实时视频传输系统的 QoE 预测**

- **作者**：Y. Sun, W. Xiong, G. Pan, S. Xu, S. Zhang, X. Chen
- **出处**：IEEE Transactions on Vehicular Technology，2025年

**a. 技术概述**

该论文提出了一套**RIC 辅助的无线实时视频 QoE 预测系统**，在真实硬件平台上实现了"网络测量 → AI 预测 → 网络控制"的闭环。系统基于 OpenAirInterface（OAI）搭建 4G/5G 基站和核心网、Edgegallery 作为 RIC 平台、USRP B210 作为射频前端，同时搭建 WebRTC 实时视频传输服务，使 6 台华为 Nexus 6P 商用手机作为 UE 接入并播放视频。UE 在移动过程中——论文通过**移动 UE 的位置来改变无线网络状态**——产生不同传输条件下的数据，RIC 服务器通过 flexRAN 反馈接口采集 RAN 侧的上下文测量数据，WebRTC 服务器同时记录视频 QoE 指标，形成"网络状态 + QoE 标签"的配对数据集。

**b. 预测模型与核心设计**

论文提出**两阶段Transformer感知与预测模型 TTPP（Transformer-based Transmission Period Prediction）** 方案：

- 第一阶段根据网络测量估计当前视频QoE；
- 第二阶段结合历史序列预测下一周期QoE；
- 通过两种数据增强方法提升模型对无线波动的鲁棒性；
- 在RIC辅助的实时视频传输原型系统中进行部署测试。

**c. 研究启示**

这项工作的价值是将分析目标从RSRP或吞吐量提升到了用户实际体验，而且不是只对QoE进行离线分类，而是预测下一时间段的体验。它证明了RAN测试结果 → 应用体验预测这条链路可以在实际系统中运行。

**（7）**[**RANGAN: GAN-empowered Anomaly Detection in 5G Cloud RAN**](https://arxiv.org/html/2508.20985v1)** 基于 GAN 的 5G 云 RAN 异常检测**

- 作者：D. Liao, J. Luo, J. Vevstad, N. Pappas（林雪平大学 & 爱立信）
- 会议：2025 IEEE Conference on Standards for Communications and Networking，CSCN 2025

**a. 技术概述**

5G 云化 RAN 每秒产生海量 KPI 数据，传统基于固定阈值和规则的异常检测方法在动态变化的网络环境中效果急剧下降。该论文提出 RANGAN，一种将生成对抗网络（GAN）与 Transformer 架构结合的无监督异常检测框架。核心思路是：用 GAN 学习 RAN 正常运行时 KPI 的分布模式，当输入数据的分布偏离所学模式时判定为异常。Transformer 的注意力机制使模型能捕捉 KPI 时序中的长距离依赖关系，而 GAN 的无监督特性使模型无需标注数据即可训练——这对标注获取困难的实际网络运维场景尤为重要。

**b. 数据处理与模型设计**

使用公开的 SpotLight 数据集（模拟 5 个 UE 在多种流量类型下的 RAN KPI，按 3GPP 标准定义，涵盖前传链路使用率和 CPU 线程调度等类别），经最小-最大归一化后，用滑动窗口将 KPI 时序切分为固定长度重叠片段。窗口大小从 20 到 60 可调。GAN 的生成器接收随机噪声产生与训练样本同维度的输出，判别器区分真实样本与生成样本，两者对抗训练。Transformer 模块嵌入生成器和判别器中，通过注意力权重使模型聚焦于输入序列中最具信息量的时间片段。

**c. 实验结果**

与 7 种基线方法（Autoencoder、Isolation Forest、LOF、OCSVM、HDBSCAN、DBSCAN、Z-Score）对比，RANGAN 在检测网络资源争用异常上取得最优，滑动窗口消融实验表明窗口越大性能越好，足够的时序上下文对于区分正常与异常行为至关重要。

**d. 研究启示**

该论文的核心价值在于验证了 GAN+Transformer 的组合在 RAN KPI 无监督异常检测中的有效性，且在与爱立信的合作背景下完成，贴近实际运维需求。论文说明异常判断不能只看某个瞬时KPI是否越过阈值。例如吞吐量突然下降，在高负载、切换和无线衰落情况下可能具有完全不同的含义。滑动窗口能够利用异常发生前后的变化轨迹。

##### 3、测试结果如何驱动“网络优化”

1. **调研思路**

要求系统根据测试或预测结果执行明确动作，例如切换小区、改变波束、启用备用链路、调整调度优先级、修改发射功率或提前缓存内容。只有当结果进入动作选择并通过新的测量验证收益时，才形成闭环优化。

1. **调研内容**

（1）

##### 4、智能通信相关

1、动态环境下的 Radio Map：2026年论文 Robust radio map prediction: An online adaptation approach with dynamic learning rates

[https://www.sciencedirect.com/science/article/pii/S0957417425045415#abs0001](https://www.sciencedirect.com/science/article/pii/S0957417425045415#abs0001)

它直接针对：**动态环境造成 radio-map prediction 性能下降**的问题。

方法不是重新训练整个模型，而是在线适配：

- feature memory
- dynamic learning rate
- online correction

从而应对实时环境变化。

这个非常值得和：

> WRF-GS / Point2Radio / Wireless World Model

放在一起理解。

1、**面向动态蜂窝网络的用户体验感知主动测试方法**

传统网络测试一般是固定规则：每100 ms测一次

或者：RSRP<−110 dBm ⇒ 触发某个事件

这存在两个问题：

第一，**大量测量其实没价值**。用户静止、信道稳定、业务正常时，每隔很短时间重复测量，得到的信息高度重复。

第二，**真正出问题的时候又可能测得不够**。例如高速移动、切换、突然遮挡、波束变化时，网络状态变化很快，固定测量周期可能捕捉不到关键瞬间。

Network testing itself becomes an optimization problem

不是只优化网络，而是首先优化：**“怎么测网络”。**

思路：**预测 QoE 正常+ 网络稳定** → 降低测试频率

**预测 QoE 即将下降** → 提高测试频率

**预测不确定** → 主动补测关键指标

**移动/切换 + QoE异常** → 触发深度网络测试

2、| 跨场景泛化 | 换一个场景不用重训，用点云+材质感知做"基础模型" | **Point2Radio**（arXiv 2607.28994）—— 跨场景无线电场基础模型 |