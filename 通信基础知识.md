1. **KPI（Key Performance Indicator，关键性能指标）**：通信网络中用来衡量网络性能好坏的核心指标。

| **常见无线网络 KPI** | **含义** | **越低越好还是越高越好** |
| --- | --- | --- |
| RSRP | 参考信号接收功率，信号有多强 | 越高越好 |
| SINR | 信号与干扰加噪声比，信号清不清楚 | 越高越好 |
| 吞吐量 | 用户实际能跑多快 | 越高越好 |
| 时延 | 数据从 A 到 B 花多长时间 | 越低越好 |
| 丢包率 | 传 100 个包丢了几个 | 越低越好 |
| BLER | 误块率，收到的数据块有多大比例是错的 | 越低越好 |
| 接入成功率 | 100 次尝试连接成功了几次 | 越高越好 |

注意 KPI 和 QoE 的区别：KPI 是网络的"体检指标"，QoE 是用户的"主观感受"。比如 SINR=20dB（KPI 很好）但视频还是卡了（QoE 很差），可能是因为视频服务器出了问题，与无线网络无关。

1. **PRB（Physical Resource Block，物理资源块）**：是 LTE/5G 空中接口上资源调度的最小单位。一个 PRB 是频域上 12 个子载波 × 时域上 1 个时隙（0.5ms）

**PRB 占用率**：在当前时刻，基站已分配出去的 PRB 数量 ÷ 基站总共可用的 PRB 数量，即**频谱资源的利用率**。

通俗说：基站的频谱带宽就像一个停车场，PRB 是停车位，PRB 占用率就是"车位使用率"。占用率 80% 说明网络比较忙但还有余量，占用率 100% 说明拥塞了，新来的用户没资源可用，只能排队等或降速。

1. **SNR（Signal-to-Noise Ratio，信噪比）**：信号功率 ÷ 噪声功率，用来衡量信号有多"干净"。**值越大越好**。
2. **MWS （Median Weekly Signature，中位数周特征）预测器：**一种简单的流量预测方法。取过去 3 周同一时刻的 KPI 值的中位数，作为下一周对应时刻的预测值。

举例：要预测下周周三 9:00 的 PRB 占用率，就把本周三 9:00、上周三 9:00、上上周三 9:00 三个值取中位数，就是预测结果。

它假设网络流量的周模式是稳定的（每个周一都差不多、每个周末也差不多），不需要训练任何模型，异常值受中位数保护不会被放大。

1. **OTA（Over-The-Air，空口）测试**：不通过线缆连接，而是在空中通过天线发射信号来测试无线设备的性能。就像测手机信号好不好，不是把网线插到手机上，而是让手机真实接收空中的无线信号来测。

传统测试用线缆直连——测信号源用射频线接到设备上，信号纯净但完全不像真实环境。OTA 测试用天线在微波暗室里模拟真实无线环境，测到的是设备真实的表现。

OTA 测试简单说就是：把被测设备放进一个**微波暗室**（墙上贴满吸波材料，像录音棚但防的是电磁波反射而非声音），周围摆一圈探头天线。测试时探头天线模拟不同方向的信号来波，暗室内没有外部干扰和反射，唯一打到 DUT 上的就是探头发出的信号。通过控制每个探头发什么信号、功率多大、时延多少，就可以在实验室里"造"出任意的无线信道环境——静态的、移动的、多径的、遮挡的都可以模拟。

1. **DUT（Device Under Test，被测设备）**：正在被测试的那个设备，比如测手机天线性能时手机就是 DUT，测基站时基站就是 DUT。
2. **OAI（OpenAirInterface）**：一套开源的 4G/5G 基站和核心网软件实现。它让普通计算机 + 一个软件无线电硬件（如 USRP B210）就能运行一个真实的蜂窝基站，手机可以像连商用基站一样连上去。研究人员用它搭建实验网络，而不需要购买昂贵的商用基站设备。